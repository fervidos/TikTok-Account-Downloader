# Copy this file to `.env` and fill in the values.
# This file is NOT used by default; it is only a template.

# Optional: MongoDB URI to track downloaded videos and avoid re-downloading.
MONGO_URI="mongodb+srv://<user>:<password>@<cluster>/tiktok_account_downloader?retryWrites=true&w=majority"
